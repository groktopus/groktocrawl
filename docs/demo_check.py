"""Demo module for the merge-blocking demonstration (issue #499).

This file intentionally carries a ``# TODO:`` marker so the ruff TD check
fails deterministically. It exists only inside the harmless, docs-only demo
PR and is removed again in Phase 2 of the demonstration.
"""


def demo_check() -> str:
    # TODO: fix — intentional marker; this demo must fail the ruff TD check.
    return "demo"
